# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here http://doc.crates.io/guide.html#cargotoml-vs-cargolock
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# By default, the tool cache is stored in ~/.cargo/registry and ~/.cargo/git
# But these directories can be overridden using CARGO_HOME and RUSTUP_HOME
# https://doc.rust-lang.org/cargo/guide/cargo-home.html
# These are used to store build cache and are safe to delete if the user desires
.cargo
.rustup

# These are generated by various rust tools, useful for tracking
*.rs.meta

# Uncomment these to remove any platform-specific files
# **/*.exe
# **/*.dll
# **/*.dylib
# **/*.so

# You can ignore debug or release builds with the following
# /target/debug/
/target/release/
/target
/Cargo.lock
/.env
.idea
